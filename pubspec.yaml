name: drone_page
description: Standalone Drone UI (no backend logic)
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  google_fonts: ^6.1.0
  geolocator: ^11.0.0
  permission_handler: ^11.3.0
  http: ^1.2.0
  image: ^4.1.3
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

